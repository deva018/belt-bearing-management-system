# Express Server Config
PORT=3000
NODE_ENV=development

# JWT security secret
JWT_SECRET=your-super-secret-random-key-here-123456

# Database configuration (defaults to workspace local)
# Set to /var/data/database.sqlite on Render with Persistent Disk
SQLITE_DB_PATH=./database.sqlite
